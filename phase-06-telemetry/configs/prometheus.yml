# Prometheus configuration for Netaform Phase 6
# Scrapes gnmic's Prometheus exporter for network telemetry

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: gnmic
    static_configs:
      - targets:
          - clab-netaform-p6-gnmic:9804
